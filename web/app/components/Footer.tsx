/** F55 — 모든 화면에, 스크롤과 무관하게 항상. */
export function Footer() {
  return (
    <div
      role="contentinfo"
      className="fixed inset-x-0 bottom-0 z-20 flex h-10 items-center justify-center border-t border-line bg-sunk text-xs text-muted"
    >
      투자 권고가 아닙니다 · 참고용 테스트
    </div>
  );
}
