# SPEC.md — krx-signal-verify

> **상태: v0.1 초안** (2026-08-31). 선행 `krx-signal-briefing` v3.1.1을 **계승하되 범위를 넓힌** 새 프로젝트다.
> 이름을 바꾸는 이유부터 적는다 — 선행은 `briefing`(나열)이라는 이름으로 시작해 `verify`(검증)를 하게 됐고,
> SPEC에 v1.1 → v2.0 → v3.0 → v3.1의 지층이 그대로 남았다. 이 문서는 **지금 하려는 일만** 적는다.
>
> **v0.2 (2026-08-31): 미확정이던 6개가 전부 확정됐다** (§2-1 V4·V5·V6b·V8·V9·V11 — 사용자 결정, 전부 추천안).
> 이제 SPEC에 열린 결정이 없다. `PLAN.md`로 넘어간다.
> 변경은 이 문서를 먼저 고치고 PLAN → DESIGN → TASKS 순으로 역추적한다 (워크스페이스 CLAUDE.md 진행 원칙 6).

---

## 1. 프로젝트 목표

`krx-signal-alerts`가 평일 08:20 KST에 보내는 신호 메일은 **기술적 조건만** 말한다.
선행 프로젝트 `krx-signal-briefing`은 그 신호가 근거를 갖는지 **하루치씩** 검증해 두 번째 메일로 보냈다.
잘 돌았지만 세 가지가 비어 있었다.

```
① 판정을 매일 내면서 그게 분별력이 있었는지 한 번도 세지 않았다
     — 점수가 "보지 않는 것" 목록에 「공시 이후의 주가」가 그대로 남아 있다
② 증거가 세 갈래(공시·뉴스·수급)뿐이라 재무·공매도를 못 본다
③ 상위 신호에 걸린 종목만 볼 수 있다 — 궁금한 종목을 넣어 볼 수가 없다
④ 어제 무슨 판정을 했는지 보려면 메일함을 뒤져야 한다
```

`krx-signal-verify`는 **검증을 계승하고 위 넷을 채운다.**

```
차트 신호 (상위)  또는  사용자가 넣은 임의 종목        ← ③ 신규
      │
      ├─ 1  DART 공시 본문      무슨 일이 있었나
      ├─ 2  네이버 뉴스          왜 그랬나
      ├─ 3  기관·외국인 수급     누가 사고 팔았나
      ├─ 4  재무 (신규)          그 회사는 어떤 상태인가        ← ② 신규
      └─ 5  공매도 (신규)        반대편에 선 돈이 있는가        ← ② 신규
      │
      ▼
  정합 / 불일치 / 무관  + 근거 서술 + 규칙 점수      ← 계승 (판정은 코드가, LLM은 설명만)
      │
      ├─ 저장  판정·점수·구성 요소를 남긴다                    ← ① 신규 (선행은 버렸다)
      ├─ 채점  5·20·60거래일 뒤 주가를 붙여 분별력을 잰다      ← ① 신규
      └─ 표면  간략 메일 + 웹 대시보드                          ← ④ 신규
```

### 이 프로젝트가 하지 않는 것 (비목표)

| 비목표 | 이유 |
|--------|------|
| **매매 판단** — 매수·매도·보류·진입 시점·목표가·손절 | 선행에서 상속. 근거 정합성 판정까지는 하되 매매 판단은 하지 않는다. 여기가 유사투자자문업 경계다 (R1) |
| **개별 종목의 미래 예측** — **이 프로젝트에서 새로 생긴 가장 큰 경계** | 적중 추적(§4-2)은 **이 시스템의 판정이 분별력이 있었는지**를 재는 것이지, 「이 종목이 오를 것」을 말하는 장치가 아니다. 둘은 한 걸음 차이라 문구·표면·저장 형태 모두에 방어를 둔다 (R2·N2) |
| **적중률로 가중치를 자동 최적화** | 표본이 하루 수십 건이고 시장 국면이 바뀐다. 자동 최적화는 과적합에 더해 **왜 그 점수인지 설명할 수 없게** 만든다 — 점수의 유일한 장점을 버리는 셈이다 (V5·R4) |
| **수신자 확장** | `RECIPIENTS`는 본인 1명, 대시보드는 SSO 뒤. 선행 R7 그대로 (R1) |
| MCP **서버** 제작 | 남이 만든 MCP 서버를 데이터 소스로 쓸 뿐이다 |
| LLM에 도구를 쥐어 주는 에이전트 루프 | 도구 호출 순서는 코드가 정한다. 선행에서 지킨 규칙 그대로 (N5) |
| 상위 `krx-signal-alerts` **스크리닝 로직** 수정 | 읽기만 한다. 예외는 dispatch 단계 하나 (계승) |
| 실시간·장중 판정 | 하루 1회 배치 + 온디맨드. 장중 변화는 다음 날 반영 |
| 카카오톡 | 본문 200자에 판정 근거가 들어가지 않는다. 메일과 웹 전용 |

---

## 2. 결정 사항

### 2-1. 확정된 결정

| ID | 항목 | 상태 | 결정 | 근거 |
|----|------|------|------|------|
| **V1** | 프로젝트 형태 | **확정** (2026-08-31) | **새 디렉토리 `krx-signal-verify/` + 새 Git 저장소.** 선행 `krx-signal-briefing`은 **새 것이 5거래일 정상 도착할 때까지 계속 운영**한다 | 사용자 결정. `krx-strategy-alerts` → `krx-signal-alerts`(2026-08-17)와 같은 취급이다. 겹치는 기간에는 메일이 두 통 온다 — 그게 비교 근거가 된다 (§9 완료 기준 ⑧) |
| **V2** | 이름 | **확정** (2026-08-31) | **`krx-signal-verify`** | 사용자 선택. 하는 일이 검증이고, 상위 `krx-signal-alerts`와 접두어가 맞는다. 테이블 접두어는 **`ksv_`** |
| **V3** | 범위 | **확정** (2026-08-31) | 선행의 검증을 계승하고 **① 판정 적중 추적 ② 증거 갈래 추가 ③ 온디맨드 검증**을 더한다. 표면은 **메일 + Next.js 웹 대시보드** | 사용자 선택 |
| **V6** | 새 증거 갈래의 범위 | **확정** (2026-08-31, 실측 근거) | **재무 + 공매도 거래량·비중.** **대차·공매도 잔고는 v1에서 뺀다** | 2026-08-31 실호출: `fnlttMultiAcnt.json`은 corp_code **15개를 한 번에** 받아 442항목을 정상 반환(44종목이면 3회). `pykrx.get_shorting_volume_by_ticker('20260828','KOSPI')`는 **944행**(공매도·매수·비중)을 1회에 준다. 반면 **`get_shorting_balance_by_ticker`는 깨져 있다** — KRX 응답 열이 바뀌어 `ISU_CD·BAL_QTY·BAL_RTO`를 못 찾는다. 없는 것을 SPEC에 적지 않는다 (R6) |
| **V4** | **「판정이 맞았다」의 정의** | **확정** (2026-08-31) | **그룹 분별력.** 개별 종목의 맞고 틀림을 따지지 않고, `정합` 군과 `불일치` 군의 **지수 대비 초과수익 분포**가 서로 다른가를 본다 (중앙값·사분위·겹침·표본 수). 관측 구간 **5·20·60거래일** | `불일치`는 "근거가 신호와 어긋난다"이지 "떨어진다"가 아니다. 개별 적중률로 채점하면 이 프로젝트는 하지 않기로 한 일(예측)을 하는 장치가 된다 — 「불일치 적중률 68%」 한 줄이면 다음 판정을 예측으로 읽는다 (R2). 대가: 표본이 쌓여야 말할 수 있어 수개월이 걸린다 |
| **V5** | 적중 결과의 가중치 반영 | **확정** (2026-08-31) | **사람이 SPEC을 고쳐서만.** 집계는 관측치로만 내놓고, 가중치 변경은 SPEC F표 → 코드 순서를 지킨다. 변경 시 `rules_version`을 올린다 | 표본이 하루 수십 건이고 시장 국면이 바뀐다. 자동 재적합은 과적합에 더해 **왜 그 점수인지 설명할 수 없게** 만든다 — 점수의 유일한 장점을 버리는 셈이다 (R4) |
| **V6b** | 공매도 수집 주체 | **확정** (2026-08-31) | **상위 `krx-stock-charts`가 수집.** 일일 갱신에 하루 2회(KOSPI·KOSDAQ)를 더해 `ksc_shorting`에 넣고, 우리는 SQL로 읽는다 | `get_shorting_volume_by_ticker`가 시장당 1회로 전 종목을 준다(KOSPI 944행 실측) — 수급(D22)과 똑같은 모양이다. 우리 쪽 의존성·자격증명·외부 호출이 0으로 유지된다. 대가: **상위 리포를 또 고친다**. 상위가 조용히 멈추면 이 갈래가 낡는다 (R5) |
| **V8** | 온디맨드 실행 경로 | **확정** (2026-08-31) | **웹 → `repository_dispatch` → Actions.** 대시보드가 요청을 `ksv_requests`에 넣고 깃허브에 dispatch를 쏜다. 워크플로가 그 요청만 처리하고 결과를 DB에 쓴다. **약 1분** | 선행에서 이미 실증한 경로다 (HTTP 204 → 20초 시작). 큐+cron은 최대 30분이라 대화가 안 되고, 별도 서버는 상시 인프라가 는다. 대가: 웹에 **리포 쓰기 토큰**이 하나 더 생긴다 (R8) |
| **V9** | **대시보드의 DB 접근** | **확정** (2026-08-31) | **`ksv_*`에 SELECT만 가진 전용 Postgres 롤 + 서버 사이드 전용.** Next.js 서버 컴포넌트/라우트 핸들러에서만 그 자격증명으로 읽는다. 브라우저에는 키가 나가지 않는다 | anon+RLS는 **2026-08-31에 닫은 구멍을 그대로 다시 여는 것**이라 배제했다(공개 anon 키로 선행 브리핑 15행이 읽혔다). service_role은 **공유 Supabase 전체**를 우회해 폭발 반경이 너무 크다 — 다른 프로젝트의 `profiles`까지. 전용 롤은 폭발 반경이 `ksv_*`로 제한된다. 대가: 롤·grant를 SQL로 직접 관리 |
| **V11** | 선행 코드 이식 범위 | **확정** (2026-08-31) | **검증된 순수 함수만 이식.** `flags`(규칙 20개) · `routine` · `verdict` · `render` 금지어 · `corp`를 **테스트와 함께** 옮긴다. 그래프·I/O·저장은 넓어진 범위에 맞춰 다시 설계 | 실측으로 값을 치른 규칙표(표본 3,000건 · 손검증 11/12)를 다시 만드는 것은 낭비다. 반대로 전부 복사하면 v1.1~v3.1의 지층이 따라와 이름을 바꾸는 이유가 사라진다 |
| **V10** | 대시보드 공개 범위 | **확정** (2026-08-31) | **Vercel SSO 배포 보호를 켠 채 둔다.** 본인 계정으로 로그인해야 열린다 | 선행 R7 v3.1 계승 — 사용자가 "지금대로 본인만"으로 확정(2026-08-31). 판정·점수·적중률이 한 화면에 모이면 선행의 정적 페이지보다 훨씬 강하게 읽힌다. **끄지 않는다** |

### 2-2. 선행에서 그대로 가져오는 것 (다시 논의하지 않는다)

선행 `krx-signal-briefing`에서 **실측으로 값을 치른** 판단들이다. 이 프로젝트에서 되풀이하지 않는다.

| 계승 항목 | 선행 근거 |
|---|---|
| **판정·점수는 코드가 낸다. LLM은 설명만 한다** | 모델에게 세라고 시켰더니 「위험 유형 2건」(실제 1건)을 지어냈다 (2026-08-30). 세라고 시키지 말고 **세어서 준다** |
| **점수에 「보지 않는 것」을 항상 함께 적는다** | 0~100 숫자는 근거보다 강하게 읽힌다 (선행 R20). 이 프로젝트에서는 「공시 이후의 주가」가 목록에서 빠지므로 (§4-2) **나머지를 더 분명히** 적어야 한다 |
| **LLM은 있으면 좋은 층이다** | 죽어도 판정·점수·증거는 나간다. LLM 호출은 노드 하나에 가둔다 |
| **정형 공시는 접고, 걸린 공시는 본문을 편다** | 공시 101건 중 66건(65%)이 정형이었다. 본문에만 오버행 18.63%·리픽싱 하한이 있다 |
| **뉴스는 종목명 + 관련도순 + 제목 필터**, 앞 글자가 한글이면 다른 회사 | `아이텍`이 `위세아이텍` 안에서 잡혔다. 적합도 64% → 100% |
| **수급·시세는 상위 DB에서 SQL로 읽는다** (새 API·새 키 없음) | 선행 D14 v2·D22. 상위가 pykrx로 하루 10회에 모은다 |
| **MCP 서버는 데이터 소스다** — 버전 고정, 도구 순서는 코드가, 실패 시 REST 폴백 | 선행 D12·D15·N14 |
| **3층 분리 · 노드 20줄 · I/O 노드는 예외를 밖으로 내지 않는다** | 예외가 새면 `record_run`에 못 가 실패 기록까지 사라진다 |
| **금지어에 걸린 서술은 다듬지 않고 버린다** | 사용자 결정 2026-08-30 — 경계를 넓히는 쪽이 더 위험하다 |
| **메일은 102,400 bytes를 넘지 않는다** (Gmail 절단) | 첫 판이 149,971 bytes로 실제로 잘렸다 |
| **저장은 나눠 보낸다** | 44행을 한 문장으로 upsert했더니 statement timeout으로 **하루치가 통째로 사라졌다** (2026-08-31) |
| **`ksv_*`에 `to anon` 정책을 만들지 않는다** | 선행에서 열어 뒀더니 공개 anon 키로 브리핑 15행이 통째로 읽혔다 (2026-08-31). §2-3 V9의 출발점 |

### 2-3. 확정 경위 — 검토했던 선택지들 (2026-08-31 확정)

> 아래 6개는 **2026-08-31에 사용자가 전부 추천안으로 확정**했다 (§2-1에 결정 내용이 있다).
> 무엇을 버렸는지 남겨 둔다 — 나중에 되돌리려는 사람이 같은 검토를 반복하지 않도록.

#### ✅ V4. 「판정이 맞았다」를 무엇으로 정의하는가 — **이 프로젝트에서 가장 중요한 결정**

`불일치`는 **"근거가 신호와 어긋난다"**이지 **"떨어진다"**가 아니다. 그런데 채점 기준을 "예측이 맞았나"로
두는 순간, 이 프로젝트는 하지 않기로 한 일(예측)을 하는 장치가 된다. 정의가 곧 경계다.

| 안 | 정의 | 얻는 것 | 대가 |
|---|---|---|---|
| **A. 그룹 분별력** **(추천)** | 개별 종목의 맞고 틀림을 따지지 않는다. **`정합`으로 판정한 종목군과 `불일치`로 판정한 종목군의 이후 시장 대비 초과수익 분포가 서로 다른가**를 본다 (중앙값 차이 · 분포 겹침) | 종목별 예측이 되지 않는다. "이 지표에 분별력이 있는가"라는 **시스템 성능** 질문에 머문다 | 표본이 쌓여야 말할 수 있다. 하루 수십 건이면 의미 있는 차이를 보는 데 수개월 |
| B. 개별 적중 | 종목마다 「맞음/틀림」을 매기고 적중률 % | 직관적이고 표본이 적어도 숫자가 나온다 | **사실상 예측 성적표다.** 화면에 「불일치 판정 적중률 68%」가 뜨면 다음 판정을 예측으로 읽게 된다 (R2) |
| C. 채점하지 않음 | 주가만 붙여 보여 주고 집계하지 않는다 | 경계가 가장 안전하다 | ①번 문제를 못 푼다 — 이 프로젝트를 새로 만드는 이유가 사라진다 |

**→ A로 확정.** 관측 구간 **5·20·60거래일**, 기준은 **소속 시장 지수 대비 초과수익** — 시장이 통째로 오른 날을 판정의 공으로 돌리지 않기 위해서다.

#### ✅ V5. 적중 결과를 가중치에 어떻게 반영하는가

| 안 | 내용 | 대가 |
|---|---|---|
| **A. 사람이 SPEC을 고쳐서만** **(추천)** | 관측치를 표로 내놓고, 가중치 변경은 SPEC F표를 고친 뒤 코드에 반영 (선행 N10 계승) | 느리다 |
| B. 반자동 | 제안값을 계산해 보여 주고 사람이 승인 | 승인이 형식이 되기 쉽다 |
| C. 자동 | 주기적으로 재적합 | **과적합** + 왜 그 점수인지 설명 불가 (비목표) |

#### ✅ V6b. 공매도를 누가 수집하는가

`pykrx.get_shorting_volume_by_ticker`는 **시장당 하루 1회**로 전 종목을 준다 — 수급(선행 D22)과 똑같은 모양이다.

| 안 | 내용 | 대가 |
|---|---|---|
| **A. 상위 `krx-stock-charts`가 수집** **(추천)** | 상위 일일 갱신에 하루 **2회**(KOSPI·KOSDAQ)를 더하고 `ksc_shorting`에 넣는다. 우리는 SQL로 읽기만 | **상위 리포를 또 고친다.** 이미 수급 때 한 번 고쳤다 |
| B. 이 프로젝트가 직접 | `pykrx`를 우리 의존성에 넣고 KRX 로그인 자격증명을 받는다 | 의존성·자격증명·실패 지점이 늘고, 상위와 같은 데이터를 두 번 받는다 |

#### ✅ V8. 온디맨드 검증은 어떤 경로로 도는가

Next.js는 파이썬 배치를 직접 부를 수 없다.

| 안 | 내용 | 지연 | 대가 |
|---|---|---|---|
| **A. 웹 → `repository_dispatch` → Actions** **(추천)** | 대시보드가 요청을 `ksv_requests`에 넣고 깃허브에 dispatch를 쏜다. 워크플로가 그 요청만 처리하고 결과를 DB에 쓴다 | **약 1분** | 선행에서 dispatch 경로를 이미 실증했다(HTTP 204 → 20초 시작). 웹에 **리포 쓰기 권한 토큰**이 하나 더 생긴다 (R8) |
| B. 큐 + 주기 cron | 요청을 쌓고 30분마다 처리 | 최대 30분 | 토큰이 안 늘지만 대화가 안 된다 |
| C. 별도 FastAPI 서버 | 상시 서버 | 즉시 | 인프라가 하나 늘고 항상 켜 둬야 한다 |
| D. CLI만 | 웹에서는 못 한다 | — | 가장 싸다. ③을 절반만 푼다 |

#### ✅ V9. 웹 대시보드가 DB를 어떻게 읽는가 — **보안상 가장 중요한 결정**

2026-08-31에 `ksb_*`의 anon 읽기를 닫았다. **공개된 anon 키**(상위 `krx-stock-charts` 웹 번들에 실려 있다)로
브리핑 15행이 통째로 읽히고 있었기 때문이다. 대시보드를 만들면 이 문제가 그대로 돌아온다.

| 안 | 내용 | 대가 |
|---|---|---|
| **A. 전용 읽기 롤 + 서버 사이드** **(추천)** | `ksv_*`에 **SELECT만** 가진 Postgres 롤을 만들고, Next.js **서버 컴포넌트/라우트 핸들러에서만** 그 자격증명으로 읽는다. 브라우저에는 키가 나가지 않는다 | 롤·grant를 직접 관리해야 한다. Supabase 콘솔 밖의 SQL |
| B. service_role 키를 서버 사이드에서 | 구현이 가장 쉽다 | 그 키는 **공유 Supabase 전체**를 우회한다 — 다른 프로젝트의 `profiles`까지. 폭발 반경이 너무 크다 |
| C. anon + RLS 정책 | 표준적인 Supabase 방식 | **오늘 닫은 구멍을 그대로 다시 연다.** 반대 |
| D. 배치가 정적 스냅샷을 만들어 올린다 | DB 접근이 0. 선행 방식의 확장 | 필터·정렬이 클라이언트 사이드로 제한되고, 데이터가 커지면 페이지가 무거워진다 |

**→ A로 확정.** C(anon+RLS)는 오늘 닫은 구멍을 다시 여는 것이라 배제했다.

#### ✅ V11. 선행 코드를 어디까지 가져오는가

| 안 | 내용 |
|---|---|
| **A. 검증된 순수 함수만 이식** **(추천)** | `flags`(규칙 20개) · `routine` · `verdict` · `render` 금지어 · `corp` 를 **테스트와 함께** 옮긴다. 그래프·I/O·저장은 넓어진 범위에 맞춰 다시 설계 |
| B. 전부 복사 후 수정 | 빠르지만 지층이 따라온다 — 이름을 바꾸는 이유가 사라진다 |
| C. 전부 새로 | 실측으로 값을 치른 규칙표까지 다시 만든다. 낭비 |

---

## 3. 데이터 소스

### 3-1. Supabase — 읽기 전용으로 재사용 (계승)

| 테이블 | 소유 | 이 프로젝트의 권한 |
|---|---|---|
| `ksa_signals` · `ksa_runs` | `krx-signal-alerts` | **SELECT만** |
| `ksc_bars` · `ksc_tickers` · `ksc_investor_flows` | `krx-stock-charts` | **SELECT만** — 시세·시총·수급, 그리고 **적중 추적의 사후 주가**(§4-2) |
| `ksc_shorting` (신설 예정) | `krx-stock-charts` | **SELECT만** — V6b가 A로 확정되면 |
| `ksb_*` | `krx-signal-briefing` (선행) | **읽지 않는다.** 병행 기간에도 서로 간섭하지 않는다 |
| `ksv_*` | **이 프로젝트** | 읽기·쓰기 |

### 3-2. OpenDART — 무료 · 인증키 40자 · 일 20,000회 (계승 + 신규)

| 용도 | 엔드포인트 | 호출 수 |
|---|---|---|
| 티커 → corp_code (계승) | `corpCode.xml` | 하루 1회 |
| 공시 목록 (계승) | `list.json` (korean-dart-mcp 경유, REST 폴백) | 종목당 1회 |
| 공시 본문 (계승) | `get_corporate_event` 계열 — 인자는 `start`/`end` (`bgn_de`면 HTTP 200 + `status:100`으로 조용히 0건) | 걸린 공시당 1회 |
| **재무 (신규)** | **`fnlttMultiAcnt.json`** — corp_code를 쉼표로 **여러 개** | **15개/회 실측** → 44종목이면 3회 |

**실측 (2026-08-31)**: corp_code 15개 요청 → `status 000` · 442항목 · **15/15 회사 응답**.
계정은 `매출액` `영업이익` `당기순이익(손실)` `자산총계` `부채총계` `자본총계` `이익잉여금` 등.
**금융사는 계정 이름이 다르다** — `순이자손익` `순수수료손익` `예수부채` `차입부채` (R7).

### 3-3. 공매도 — pykrx (신규)

| 함수 | 축 | 실측 (2026-08-31) | 쓸 수 있나 |
|---|---|---|---|
| `get_shorting_volume_by_ticker(date, market)` | **날짜축 — 전 종목** | KOSPI `20260828` **944행**, 열 `공매도·매수·비중` | **된다.** 시장당 하루 1회 |
| `get_shorting_status_by_date(from, to, ticker)` | 종목축 | 18행, 열 `거래량·잔고수량·거래대금·잔고금액` | **온디맨드에만.** 종목당 1회라 배치엔 안 맞는다 |
| `get_shorting_balance_by_ticker(date, market)` | 날짜축 | **깨져 있다** — `ISU_CD·BAL_QTY·LIST_SHRS·BAL_AMT·MKTCAP·BAL_RTO`를 못 찾는다 | **안 된다** (R6) |

### 3-4. MCP 서버 2종 (계승)

`korean-dart-mcp`(공시·anomaly·insider) · `@isnow890/naver-search-mcp`(뉴스). `npx -y` stdio, 버전 고정.
korea-stock-mcp는 쓰지 않는다 — 시총은 상위가 채우고 순매수 도구는 애초에 없다 (선행 D14 v2).

---

## 4. 기능 요구사항

### 4-1. 계승 — 검증 파이프라인

선행 `krx-signal-briefing` v3.1.1의 F1~F20을 그대로 가져온다. 여기서는 **바뀌는 것만** 적는다.

| ID | 요구사항 |
|----|----------|
| **F1** | **게이트** — 상위 `ksa_runs` 오늘 행을 보고 판정. 없으면 1분×10회 뒤 `gate_timeout`. `stale_data`면 신호 없이 `[검증 없음]` 발송. **이벤트가 아니라 DB를 믿는다** (계승, 2026-08-31 실증) |
| **F2** | **대상** — 그날 `suppressed = false`인 `ksa_signals` 전부. `sent_email`은 상위가 저장하지 않으므로 쓰지 않는다 (계승) |
| **F3~F9** | 공시 조회·규칙표 판정(20개)·정형 공시 접기·본문 파싱·뉴스·수급 — 선행 F3~F17 그대로 (계승, §2-2) |
| **F10** | **판정과 점수** — `verdict.judge()` 순수 함수. `정합`/`불일치`/`무관` + 0~100점 + 구성 요소. **LLM이 바꾸지 못한다** (계승) |
| **F10b** | **(변경)** 점수의 「보지 않는 것」 목록에서 **「공시 이후의 주가」를 뺀다** — §4-2가 그것을 보기 때문이다. 나머지(실적·밸류에이션 → F30이 일부 채운다 · 업황 · 시장 전체 흐름)는 그대로 적는다 |
| **F11** | **근거 서술** — Claude Opus 5 하루 1회 일괄, 종목당 2,000자 이내(상한이지 목표가 아니다). 금지어·티커·건수를 코드가 대조해 걸린 항목만 버린다 (계승) |

### 4-2. 신규 — 판정 이력과 적중 추적

> V4가 **그룹 분별력**으로 확정됐다 (2026-08-31). F22~F24는 그 정의를 따른다.

| ID | 요구사항 |
|----|----------|
| **F20** | **판정을 저장한다.** `ksv_verdicts`에 `d` · `ticker` · `stand` · `score` · `parts`(구성 요소와 가중치) · `blind_spots` · `rules_version`을 남긴다. **선행은 렌더 때 계산하고 버렸다** — 그래서 이력이 없다 (2026-08-31 확인). 가중치를 고치면 `rules_version`이 올라가고, 과거 판정은 **그때의 산식으로 남는다** |
| **F21** | **콜드스타트 — 과거 판정을 소급 재현한다.** 판정은 **LLM 없이 코드만으로** 나므로 소급 비용은 DART 조회뿐이다. `ksa_signals`의 과거 신호를 **60~120거래일**분 소급 판정해 추적의 출발 표본을 만든다. 근거 서술(F11)은 소급하지 않는다 — 비용이 크고 채점에 쓰이지 않는다 |
| **F22** | **사후 주가를 붙인다.** 판정일로부터 **5·20·60거래일** 뒤의 종가를 `ksc_bars`에서 읽어 `ksv_outcomes`에 채운다. 아직 도래하지 않은 구간은 `null`로 두고 매일 채운다 — **판정을 되돌아보는 배치이지 예측하는 배치가 아니다** |
| **F23** | **기준 대비로 정규화한다.** 절대 등락이 아니라 **소속 시장 지수 대비 초과수익**으로 기록한다. 시장이 통째로 오른 날을 판정의 공으로 돌리지 않기 위해서다 (V4) |
| **F24** | **분별력을 집계한다.** `정합` 군과 `불일치` 군의 초과수익 **분포**를 비교한다 — 중앙값·사분위·분포 겹침·표본 수. **적중률(%) 한 숫자로 요약하지 않는다** (R2). 표본이 기준 미만이면 **수치 대신 「표본 부족(n=…)」**을 표시한다 |
| **F25** | **가중치는 사람이 고친다** (V5 A안 확정 시). 집계 결과는 관측치로만 내놓고, 변경은 SPEC F표 → 코드 순서를 지킨다. 변경 시 `rules_version`을 올린다 |
| **F26** | **점수 산식의 이력을 문서로 남긴다** — 언제·왜·무엇을 바꿨는지. 과거 판정과 현재 판정을 같은 자로 잰 것처럼 섞지 않는다 |

### 4-3. 신규 — 증거 갈래 추가

| ID | 요구사항 |
|----|----------|
| **F30** | **재무를 붙인다** — `fnlttMultiAcnt.json`으로 신호 종목의 최근 보고서 주요계정을 가져온다(15개/회). 표시는 **사실만**: 매출액·영업이익·당기순이익의 전기 대비, 부채비율, 이익잉여금 부호. **밸류에이션(PER·PBR)은 v1에서 계산하지 않는다** — 시총은 있지만 주식 수·지배주주지분 처리가 업종마다 달라 잘못 쓰기 쉽다 |
| **F31** | **금융사는 다르게 읽는다** — 계정이 `순이자손익`·`예수부채`·`차입부채`로 온다 (실측). 매출액·영업이익을 못 찾으면 **비워 두고 그렇다고 적는다.** 억지로 매핑하지 않는다 (R7) |
| **F32** | **공매도를 붙인다** — 일별 `공매도 거래량`과 **비중(%)**, 최근 20거래일 추이. 상위가 수집하면 SQL로 읽는다 (V6b) |
| **F33** | **잔고는 v1에 없다** — `get_shorting_balance_by_ticker`가 깨져 있다 (실측). **없는 것을 있는 척하지 않는다.** 온디맨드(§4-4)에서는 `get_shorting_status_by_date`로 종목 단위 잔고를 볼 수 있으므로 거기서만 제공한다 |
| **F34** | 새 갈래도 **없어도 되는 층**이다 — 실패하면 그 줄만 비우고 「생략」으로 표기한다. 메일·판정을 막지 않는다 (계승) |

### 4-4. 신규 — 온디맨드 검증

> V8이 **웹 → dispatch → Actions**로 확정됐다 (2026-08-31).

| ID | 요구사항 |
|----|----------|
| **F40** | **CLI로 임의 종목을 검증한다** — `python -m verify.main --ticker 227950`. 상위 신호가 없어도 돈다. 신호가 없으므로 **차트 조건 줄이 비고**, 판정은 「신호 없음 — 증거만 정리」로 표시한다. 판정 3분류를 억지로 붙이지 않는다 |
| **F41** | **웹에서 요청한다** (V8 A안) — 대시보드가 `ksv_requests`에 넣고 `repository_dispatch`로 워크플로를 깨운다. 약 1분 |
| **F42** | **요청에 상한을 둔다** — 하루 요청 수, 동시 처리 1건. 온디맨드는 **요청마다 LLM 비용**이 든다 (R9) |
| **F43** | **결과는 배치와 같은 표에 남긴다** — `ksv_verdicts`에 `source = 'ondemand'`로. 다만 **적중 추적 집계에서는 배치와 섞지 않는다** — 사용자가 궁금해서 넣은 종목은 표본이 편향돼 있다 |

### 4-5. 신규 — 표면: 메일 + 웹 대시보드

> V9가 **전용 읽기 롤 + 서버 사이드**로 확정됐다 (2026-08-31).

| ID | 요구사항 |
|----|----------|
| **F50** | **메일은 간략하게** — 판정·점수·근거 발췌 + 대시보드 링크. 102,400 bytes 예산과 3단계 접기는 계승. **판정·점수·공시·한계 문구는 어느 단계에서도 접지 않는다** |
| **F51** | **오늘 화면** — 그날 판정을 표로. 정렬·필터(판정·전략·점수) |
| **F52** | **종목 화면** — 증거 다섯 갈래를 한 화면에. 공시는 DART 원문 링크 필수 (계승 N2) |
| **F53** | **이력 화면** — 날짜·종목으로 과거 판정을 되짚는다. 판정 시점의 `rules_version`을 함께 보여 준다 |
| **F54** | **분별력 화면** — F24의 분포 비교. **적중률 %를 크게 띄우지 않는다.** 표본 수와 「이것은 예측이 아니다」를 같은 화면에 둔다 (R2) |
| **F55** | **모든 화면에 「투자 권고가 아닙니다 · 참고용 테스트」** 를 상시 표기하고 `noindex`를 건다. SSO가 풀리는 날을 대비한 이중 방어 (계승 F20b) |

---

## 5. 데이터 모델 — 새로 만드는 것 (`ksv_` 접두어)

| 테이블 | 내용 | 키 |
|---|---|---|
| `ksv_verdicts` | 판정 1건 — `d` · `ticker` · `source`(batch/ondemand) · `stand` · `score` · `parts`(jsonb) · `blind_spots` · `rules_version` | PK `(d, ticker, source)` |
| `ksv_evidence` | 그날 그 종목의 증거 — 공시·본문·뉴스·수급·재무·공매도 (jsonb) | PK `(d, ticker)` |
| `ksv_outcomes` | 사후 주가 — `d` · `ticker` · `h5` · `h20` · `h60` · 각 구간의 시장 대비 초과수익 · `filled_at` | PK `(d, ticker)` |
| `ksv_runs` | 실행 기록 — 게이트 판정·신호 수·판정 수·실패 사유 | PK `run_at` |
| `ksv_requests` | 온디맨드 요청 (V8 A안) — `requested_at` · `ticker` · `status` · `result_d` | PK `id` |

**RLS**: 다섯 테이블 모두 RLS를 켜고 **정책을 만들지 않는다.** 읽기 경로는 V9에서 정한다.
`to anon` 정책은 어떤 경우에도 만들지 않는다 (§2-2, 선행 2026-08-31 사고).

---

## 6. 제약과 리스크

| ID | 제약 | 영향 | 대응 |
|----|------|------|------|
| **R1** | **유사투자자문업 경계 — 선행보다 커졌다** | 판정에 더해 **적중 이력**과 **웹 화면**이 생긴다. 「이 판정은 과거에 잘 맞았다」는 사실상 예측력 주장이다 | ① `RECIPIENTS` 본인 1명 ② Vercel SSO 보호 유지(V10) ③ 모든 화면에 투자권고 아님 명시 + `noindex`(F55) ④ 매매 판단 금지어(N1) ⑤ **적중은 그룹 분별력으로만**(V4 A안) |
| **R2** | **적중 추적이 예측 장치로 읽힌다 — 이 프로젝트 최대 리스크** | 「불일치 적중률 68%」 한 줄이면 다음 판정을 예측으로 읽게 된다 | **적중률 %를 만들지 않는다**(F24). 분포·표본 수·겹침으로만 표시. 표본 부족이면 수치 대신 「표본 부족(n=…)」. V4 확정 전 구현 착수 금지 |
| **R3** | **대시보드가 anon 구멍을 다시 연다** | 2026-08-31에 닫은 것 | **V9에서 C안(anon+RLS)을 배제**하고 확정. `ksv_*`는 정책 없이 생성하고, 회귀 테스트로 `to anon` 정책 부활을 막는다 (선행 `test_schema.py` 계승) |
| **R4** | **적중 데이터로 가중치를 맞추면 과적합** | 하루 수십 건 표본, 시장 국면 변화 | V5 A안(사람이 SPEC을 고쳐서만). `rules_version`으로 과거 판정을 그때 산식으로 고정(F20) |
| **R5** | **상위 프로젝트 의존이 하나 더 늘어난다** | 수급에 이어 공매도까지 `krx-stock-charts`에 기댄다. 상위가 2주간 조용히 멈춘 적이 있다 (2026-08-18~08-31) | 새 갈래는 **없어도 되는 층**(F34). 상위 `ksc_meta`로 신선도를 확인하고, 낡았으면 **그렇다고 표시**한다 — 조용히 빠지지 않는다 |
| **R6** | **pykrx 공매도 잔고 함수가 깨져 있다** | 실측 확인 (2026-08-31) | v1 범위에서 뺀다(F33). 온디맨드는 종목축 함수로 대체. pykrx가 고쳐지면 SPEC을 고쳐 넣는다 |
| **R7** | **금융사 재무 계정이 다르다** | `매출액`·`영업이익`이 없고 `순이자손익`·`예수부채`가 온다 (실측) | 못 찾으면 **비우고 그렇다고 적는다**(F31). 억지 매핑 금지 |
| **R8** | **웹에 리포 쓰기 토큰이 생긴다** (V8 A안) | dispatch를 쏘려면 필요 | fine-grained PAT · 대상 리포 1개 · Contents write만. 서버 사이드에서만 쓴다. 선행 R13과 같은 취급 |
| **R9** | **온디맨드가 요청마다 LLM 비용을 태운다** | 배치는 하루 1회 일괄이라 예측 가능했다 | 하루 요청 상한 + 동시 1건(F42). 판정만 먼저 보여 주고 근거 서술은 선택적으로 |
| **R10** | **적중 추적은 이력이 0에서 시작한다** | 선행이 판정을 저장하지 않았고 `ksb_briefings`에 1일치뿐이다 (2026-08-31 확인) | F21 소급 재현으로 출발 표본을 만든다. **60거래일 지표는 60거래일이 지나야 나온다** — 완료 기준에 이 시간을 반영한다(§9) |
| **R11** | **병행 운영 기간에 메일이 두 통** | V1 — 선행을 계속 돌린다 | 제목으로 구분(`[검증]` vs `[브리핑]`). 겹치는 기간은 **비교 근거**로 쓴다(§9 ⑧). 새 것이 5거래일 정상 도착하면 선행을 끈다 |
| **R12** | `evidence` 계약 의존 (계승) | 상위가 키를 바꾸면 깨진다 | `SignalRow`가 `null`·쉼표 낀 숫자·목록 아님을 전부 빈 값으로 떨어뜨린다. **직접 `.get()`으로 파헤치지 않는다** |
| **R13** | 새 키 없음 확인 | 재무는 기존 `DART_API_KEY`, 공매도는 상위의 기존 KRX 자격증명 | **v1에 새 외부 키가 필요 없다.** V8 A안의 PAT만 추가 |

---

## 7. 비기능 요구사항

| ID | 요구사항 |
|----|----------|
| **N1** | **문구 규칙** (계승) — 사실과 근거 정합성은 되고 매매 판단은 안 된다. 금지어: `추천` `매수` `매도` `보류` `목표가` `손절` `여력` `이탈` `진입` `비중` · 단독 `없음`. `순매수`·`순매도`·`매수세`·`매도세`는 허용 복합어로 먼저 지우고 검사한다. 공시·뉴스 제목 원문은 예외. 메일·프롬프트·출력 검증 **세 곳**에 같은 규칙 |
| **N2** | **(신규) 적중 문구 규칙** — 개별 종목에 「맞았다/틀렸다」를 쓰지 않는다. 집계에도 `적중률` `승률` `수익률`을 쓰지 않고 **`초과수익 분포`·`분별력`·`표본 수`**로 적는다. 렌더 테스트가 잡는다 |
| **N3** | 모든 공시 항목에 DART 원문 링크 필수 (계승) |
| **N4** | **3층 분리** (계승) — 그래프 층만 LangGraph를 안다. 도메인은 순수 함수(TDD 대상). I/O만 부수효과. **LangGraph를 걷어내도 도메인이 살아 있어야 한다** |
| **N5** | **MCP 서버는 데이터 소스다** (계승) — 도구 순서는 코드가 정하고 LLM에 도구를 주지 않는다. 버전 고정 |
| **N6** | 노드는 20줄 이내. **I/O 노드는 예외를 밖으로 내지 않는다** — 실패 판정은 `finalize` 한 곳. fan-out에는 reducer 필수 (계승) |
| **N7** | 멱등 — PK upsert. **저장은 나눠 보낸다** (선행에서 하루치가 통째로 사라졌다) |
| **N8** | **되살리는 열이 저장하는 열보다 적으면 재실행이 지운다** (계승) — 왕복 테스트로 잠근다 |
| **N9** | 메일 102,400 bytes 이내, 실제 `EmailMessage` 크기로 테스트 |
| **N10** | 시크릿은 `.env`/Secrets에만. **DART 키는 URL 쿼리에 실리므로 예외 메시지에서 URL을 마스킹한다.** 히스토리·로그에 값이 없는지 배포 전 대조 (계승) |
| **N11** | **`ksv_*`에 `to anon` 정책을 만들지 않는다.** 스키마 회귀 테스트로 고정 (선행 사고) |
| **N12** | 판정 규칙 변경은 SPEC F표를 먼저 고친다. 코드의 규칙표는 SPEC의 사본이다 |
| **N13** | **LLM 출력은 코드가 검증한다** — 금지어 · 길이 · 입력에 있는 티커 · **우리가 세어 준 건수**. 모델에게 세라고 시키지 않는다 |
| **N14** | 검증 3종: `ruff check .` · `mypy`(strict) · `pytest`. 웹은 `npm run lint` · `npm test` · `npm run build`. 외부 I/O는 전부 mock |
| **N15** | 배포 전 보안 점검 필수 — 보안 리뷰 · 히스토리 시크릿 스캔 · 로그 마스킹 · 워크플로 권한 · **`ksv_*` RLS** (워크스페이스 CLAUDE.md) |
| **N16** | 그래프 구조를 문서로 자동 생성 (`docs/GRAPH.md`). **설계도(`docs/diagrams/*.dot`)는 손그림이라 조용히 낡는다** — 간선을 자동 생성본과 대조한다 (선행에서 셋 다 낡아 있었다) |

---

## 8. 기술 스택

| 층 | 스택 |
|----|------|
| 배치 오케스트레이션 | **LangGraph** (계승) |
| 배치 | Python 3.11+ · `supabase-py` + `psycopg` · 표준 라이브러리 HTTP/zip/XML |
| LLM | **Claude Opus 5 `claude-opus-5`** · 공식 `anthropic` SDK · 스트리밍 · 하루 1회 일괄 |
| MCP | korean-dart-mcp · @isnow890/naver-search-mcp — `npx -y` stdio, 버전 고정. Node 20.19+ |
| 저장 | Supabase PostgreSQL (기존 프로젝트, 접두어 `ksv_`) |
| 트리거 | 상위 `alert.yml` → `repository_dispatch` (주) · 예비 cron · DB 게이트 (계승) · **온디맨드 dispatch** (V8) |
| 알림 | Gmail SMTP |
| **웹** | **Next.js 16 + TS + Tailwind** (워크스페이스 다른 프로젝트와 동일) · Vercel · **SSO 보호 유지** · DB 접근은 V9 |
| 테스트 | pytest · ruff · mypy(strict) / Vitest · ESLint · tsc |

---

## 9. 완료 기준

1. 평일 연속 **5거래일**, 신호 메일 뒤에 검증 메일이 도착 (0건·데이터 지연인 날 포함)
2. `불일치`로 판정된 **모든 건**의 원문 링크를 열어 규칙·본문과 일치하는지 확인 — 오탐 0건이 목표
3. **새 갈래 손검증** — 재무 5종목을 DART 원문과 대조(금융사 1종목 포함), 공매도 5종목을 KRX와 대조
4. 금지어 테스트(N1)·**적중 문구 테스트(N2)**·링크 필수(N3) 통과
5. **트리거 4경로** — dispatch · 예비 cron no-op · `gate_timeout` · **온디맨드 요청 → 1분 내 결과** (V8 A안 시)
6. **판정 저장·소급 재현** — `ksv_verdicts`에 60거래일 이상 소급 표본이 쌓이고, `rules_version`이 붙어 있다
7. **적중 추적은 시간이 필요하다** — 5거래일 지표는 첫 주에, **20거래일은 1개월, 60거래일은 3개월 뒤** 처음 나온다. 그때까지 화면은 「표본 부족(n=…)」을 정직하게 표시한다 (R10)
8. **두 시스템 대조** — 병행 기간 중 같은 날 선행 브리핑과 새 검증의 판정이 어긋나는 건을 손으로 확인. 어긋나면 원인을 기록한다
9. 검증 3종(파이썬·웹) 통과 · `docs/GRAPH.md` 최신 · **설계도 간선 대조 일치**(N16) · 배포 전 보안 점검 완료(N15)
10. **`ksv_*`에 anon 접근이 없음을 실제 키로 확인** — `curl -H "apikey: <anon>"` 이 빈 배열을 돌려준다 (R3)

---

## 10. 참고 — 선행 프로젝트

| 프로젝트 | 관계 |
|---|---|
| `../krx-signal-alerts/` | **신호 공급자.** 읽기만 한다. 예외는 `alert.yml`의 dispatch 단계 |
| `../krx-stock-charts/` | **시세·수급·(예정)공매도 공급자.** 읽기만 한다. V6b A안이면 상위에 수집 단계를 더한다 |
| `../krx-signal-briefing/` | **직계 선행.** 검증 로직·실측 기록·트러블슈팅이 전부 여기 있다. `docs/TASKS.md`의 「트러블슈팅 기록」과 「측정 기록」은 이 프로젝트의 출발 자산이다. **새 것이 5거래일 정상 도착하면 끈다** (V1) |

---

## 11. 변경 이력

| 판 | 일자 | 내용 |
|---|---|---|
| v0.2 | 2026-08-31 | **미확정 6개 전부 확정** (V4 그룹 분별력 · V5 사람이 수정 · V6b 상위 수집 · V8 dispatch · V9 전용 읽기 롤 · V11 순수 함수만 이식). §2-3은 버린 선택지의 기록으로 남긴다. **PLAN 착수 가능** |
| v0.1 | 2026-08-31 | 초안. V1·V2·V3·V6·V10 확정, V4·V5·V6b·V8·V9·V11 미확정. 실측 근거: `fnlttMultiAcnt` 15개/회 · pykrx 공매도 거래량 944행/회 · 공매도 잔고 함수 파손 · 선행이 판정을 저장하지 않아 이력 0 |
